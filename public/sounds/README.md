# Sound assets

Add audio files here for portfolio sound effects:

- `success.mp3` — played on contact form submit (short ~0.5s)
- `click.mp3` — optional subtle click for primary buttons

Use royalty-free sources. If files are missing, the sound system gracefully no-ops.
