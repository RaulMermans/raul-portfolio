# react-simple-maps@3 has not expanded its peer range to React 19 yet. The
# application is covered by browser smoke tests, so keep CI and local installs
# reproducible while that upstream declaration catches up.
legacy-peer-deps=true
